const product_metrics = async (strapi) => {
  switch (key) {
    case value:
      break;

    default:
      break;
  }
};

const view_count = async (strapi) => {
  const update = await strapi.db.query("api::product-metric.product-metric");
};
